{
  "name": "one-point-solutions-root",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix frontend && npm install --prefix backend",
    "dev": "concurrently \"npm run start:backend\" \"npm run dev --prefix frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "start:backend": "npm run start:full --prefix backend",
    "setup:backend": "npm run setup --prefix backend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
